fun main(args: Array<String>) {
	// when used if as an expression 'else' clause is mandatory.
	var x = 42;
	println(if (x < 50) "the given value is less than 50" else "given value is greater than 50");
}